{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "azdEnvName": { "value": "${AZURE_ENV_NAME}" },
      "applicationName": { "value": "${AZURE_ENV_NAME}" },
      "existing_LogAnalytics_Name": { "value": "${APP_NAME_NO_DASHES}-log-${ENVIRONMENT_NAME}" },
      "existing_LogAnalytics_ResourceGroupName": { "value": "${AZURE_RESOURCE_GROUP}" },
      "existing_AppInsights_Name": { "value": "${APP_NAME_NO_DASHES}-appi-${ENVIRONMENT_NAME}" },
      "existing_ACR_Name": { "value": "${APP_NAME_NO_DASHES}cr${ENVIRONMENT_NAME}" },
      "existing_ACR_ResourceGroupName": { "value": "${AZURE_RESOURCE_GROUP}" },
      "existing_Identity_Name": { "value": "${APP_NAME_NO_DASHES}-app-id" },
      "existing_KeyVault_Name": { "value": "${APP_NAME_NO_DASHES}kv${ENVIRONMENT_NAME}" },
      "existing_ManagedAppEnv_Name": { "value": "${APP_NAME_NO_DASHES}-cae-${ENVIRONMENT_NAME}" },
      "existing_ManagedAppEnv_ResourceGroupName": { "value": "${AZURE_RESOURCE_GROUP}" },
      "existing_Cosmos_Name": { "value": "${APP_NAME_NO_DASHES}-cosmos-${ENVIRONMENT_NAME}" },
      "existing_OpenAI_Name": { "value": "${APP_NAME_NO_DASHES}-cog-${ENVIRONMENT_NAME}" },
      "existing_OpenAI_ResourceGroupName": { "value": "${AZURE_RESOURCE_GROUP}" },
      "existing_DocumentIntelligence_Name": { "value": "${APP_NAME_NO_DASHES}-cog-fr-${ENVIRONMENT_NAME}" },
      "existing_DocumentIntelligence_RG_Name": { "value": "${AZURE_RESOURCE_GROUP}" },
      "existing_StorageAccount_Name": { "value": "${APP_NAME_NO_DASHES}st${ENVIRONMENT_NAME}" },
      "existing_SearchService_Name": { "value": "${APP_NAME_NO_DASHES}-srch-${ENVIRONMENT_NAME}" },
      "existing_Vnet_Name": { "value": "${APP_NAME_NO_DASHES}-vnet-${ENVIRONMENT_NAME}" },
      "useManagedIdentityResourceAccess": { "value": "true" }
    }
  }